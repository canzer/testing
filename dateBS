<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Select Date</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1>Select Date</h1>
        <form method="POST" action="/select_date">
            <div class="form-group">
                <label for="selected_date">Select Date:</label>
                <select class="form-control" id="selected_date" name="selected_date">
                    {% for date in dates %}
                    <option>{{ date }}</option>
                    {% endfor %}
                </select>
            </div>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
        {% if users_data %}
        <h2>Users Data for Selected Date</h2>
        <table class="table">
            <thead>
                <tr>
                    <th>Source</th>
                    <th>Destination</th>
                    <th>User</th>
                    <th>Event ID</th>
                    <th>Time</th>
                </tr>
            </thead>
            <tbody>
                {% for user_data in users_data %}
                <tr>
                    <td>{{ user_data.source }}</td>
                    <td>{{ user_data.destination }}</td>
                    <td>{{ user_data.user }}</td>
                    <td>{{ user_data.eventid }}</td>
                    <td>{{ user_data.time }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
        {% endif %}
    </div>
    <!-- Bootstrap JS -->
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
